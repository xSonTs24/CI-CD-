# products-svc.yml
apiVersion: v1
kind: Service
metadata:
  name: products-svc
  namespace: microapp
spec:
  selector:
    app: products
    version: blue             # ← aquí está el switch
  ports:
  - port: 5003
    targetPort: 5003
  type: ClusterIP